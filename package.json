{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "7.2.0"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "rm -rf packages/token/build"
  }
}
