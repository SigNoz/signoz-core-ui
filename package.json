{
  "name": "@signozhq/ui-core",
  "private": false,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "7.2.0"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "rm -rf packages/token/build"
  }
}
